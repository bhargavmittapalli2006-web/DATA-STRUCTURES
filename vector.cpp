#include<bits/stdc++.h>
using namespace std;
int main(){
vector<int> numbers;
numbers.push_back(3);
numbers.push_back(1);
numbers.push_back(8);
cout<<"size of dynamic array is"<<numbers.size()<<endl;
cout<<"capacity of dynamic array is"<<numbers.capacity();
//capacity increses like if we add one element in array then capict is two next we add one more element then capacity dooubles

}